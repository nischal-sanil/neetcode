{
  "slug": "subsets",
  "summary": "Given an array of distinct integers, produce every possible subset (the power set).\n\nThe result must include the empty subset and the full array itself. No subset may be repeated, and the subsets can be returned in any order.",
  "examples": [
    {
      "input": "nums = [1, 2, 3]",
      "output": "[[], [1], [2], [3], [1, 2], [1, 3], [2, 3], [1, 2, 3]]",
      "explanation": "Every combination of choosing or skipping each element gives 2^3 = 8 subsets."
    },
    {
      "input": "nums = [0]",
      "output": "[[], [0]]"
    }
  ],
  "constraints": [
    "1 <= nums.length <= 10",
    "-10 <= nums[i] <= 10",
    "All values in nums are unique",
    "The output may list subsets in any order"
  ]
}
